name: Publish Legacy-Domain Image

on:
  push:
    branches:
      - release/legacy-domain
  workflow_dispatch:
    inputs:
      expected_sha:
        description: Full 40-character SHA to publish
        required: true
        type: string
      confirmation:
        description: Type publish-legacy-domain-image to confirm image publication only
        required: true
        type: string

permissions:
  contents: read
  packages: write

concurrency:
  group: legacy-domain-image-publish
  cancel-in-progress: false

jobs:
  publish:
    name: Build and publish legacy-domain image
    runs-on: ubuntu-latest
    timeout-minutes: 40
    environment: legacy-domain-image-publish
    env:
      ALLOWED_BRANCH: release/legacy-domain
      TARGET_ORIGIN: https://legacy.jpvbootcamp.com

    steps:
      - name: Validate branch and dispatch boundary
        shell: bash
        env:
          DISPATCH_SHA: ${{ inputs.expected_sha }}
          DISPATCH_CONFIRMATION: ${{ inputs.confirmation }}
        run: |
          set -euo pipefail
          if [ "${GITHUB_REF_NAME}" != "$ALLOWED_BRANCH" ]; then
            echo "PUBLISH-DENIED: only $ALLOWED_BRANCH is permitted" >&2
            exit 1
          fi
          if [ "${GITHUB_EVENT_NAME}" = "workflow_dispatch" ]; then
            if ! printf '%s' "$DISPATCH_SHA" | grep -qE '^[0-9a-f]{40}$'; then
              echo "PUBLISH-DENIED: expected_sha must be a full lowercase 40-character SHA" >&2
              exit 1
            fi
            if [ "$DISPATCH_CONFIRMATION" != "publish-legacy-domain-image" ]; then
              echo "PUBLISH-DENIED: confirmation phrase is invalid" >&2
              exit 1
            fi
          fi
          echo "Legacy-domain image lane: branch=$ALLOWED_BRANCH origin=$TARGET_ORIGIN"
          echo "main is not an allowed source or deployment target"

      - name: Check out exact source
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0

      - name: Verify source branch and SHA
        shell: bash
        env:
          DISPATCH_SHA: ${{ inputs.expected_sha }}
        run: |
          set -euo pipefail
          actual_sha="$(git rev-parse HEAD)"
          if [ "$GITHUB_EVENT_NAME" = "workflow_dispatch" ] && [ "$actual_sha" != "$DISPATCH_SHA" ]; then
            echo "PUBLISH-DENIED: checked out SHA does not match requested SHA" >&2
            exit 1
          fi
          if ! git merge-base --is-ancestor "$actual_sha" "origin/$ALLOWED_BRANCH"; then
            echo "PUBLISH-DENIED: source SHA is not reachable from $ALLOWED_BRANCH" >&2
            exit 1
          fi
          echo "Source SHA verified: $actual_sha"

      - name: Set up pnpm
        uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4
        with:
          version: 10.33.0

      - name: Set up Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '20'
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Validate legacy runtime contract
        run: pnpm exec tsx scripts/legacy_domain_runtime_contract.test.ts

      - name: Build application
        env:
          APP_BASE_URL: https://legacy.jpvbootcamp.com
          NEXT_PUBLIC_APP_URL: https://legacy.jpvbootcamp.com
          NEXT_PUBLIC_SERVER_URL: https://legacy.jpvbootcamp.com
          NEXT_PUBLIC_APP_DOMAIN: legacy.jpvbootcamp.com
        run: pnpm run build

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3

      - name: Log in to GHCR
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}

      - name: Build and publish immutable legacy image
        uses: docker/build-push-action@ca052bb54ab0790a636c9b5f226502c73d547a25 # v6
        with:
          context: .
          file: Dockerfile.legacy
          platforms: linux/amd64
          push: true
          tags: ghcr.io/${{ github.repository }}:${{ github.sha }}-legacy
          build-args: |
            NEXT_PUBLIC_APP_URL=https://legacy.jpvbootcamp.com
            APP_BASE_URL=https://legacy.jpvbootcamp.com
            NEXT_PUBLIC_SERVER_URL=https://legacy.jpvbootcamp.com
            NEXT_PUBLIC_APP_DOMAIN=legacy.jpvbootcamp.com
          labels: |
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.created=${{ github.event.head_commit.timestamp }}
            org.opencontainers.image.version=${{ github.sha }}-legacy
            dev.jpvbootcamp.legacy-origin=https://legacy.jpvbootcamp.com
            dev.jpvbootcamp.branch=release/legacy-domain
          cache-from: type=gha,scope=legacy-domain-image-publish
          cache-to: type=gha,mode=max,scope=legacy-domain-image-publish

      - name: Report image reference
        run: |
          echo "Published image: ghcr.io/${{ github.repository }}:${{ github.sha }}-legacy"
          echo "Publication only: no Dokploy API call, DNS change, migration, or database write was performed."
