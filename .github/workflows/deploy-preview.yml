# Deploy feature branches to preview/staging environment.
# - Builds Docker image on GitHub
# - Pushes to GHCR with branch tag
# - Publishes a Docker image for the preview app
# - External preview automation may redeploy after the image is published
# - The runtime must keep STARTUP_MODE=application-only unless database deployment is separately approved
# - Push, Payload migrations, Prisma startup behavior, provider email delivery, and deployment remain independent approvals
#
# Required GitHub secrets:
#   DOKPLOY_PREVIEW_APP_ID — Dokploy application ID for the preview/staging app
#   DOKPLOY_API_KEY        — Dokploy server API key
#   DOKPLOY_API_BASE_URL   — Dokploy API base URL (e.g., https://dokploy.prochat.tools)

name: Build and Deploy to Preview

on:
  push:
    branches:
      - 'feature/**'
      - 'pr/**'

jobs:
  build-and-deploy-preview:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Determine Docker tag
        id: docker-tag
        run: |
          if [ "${{ github.ref_name }}" = "main" ]; then
            TAG="latest"
          else
            TAG=$(echo "${{ github.ref_name }}" | sed 's|/|-|g')
          fi
          echo "tag=$TAG" >> $GITHUB_OUTPUT

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          platforms: linux/amd64
          push: true
          tags: |
            ghcr.io/${{ github.repository }}:${{ steps.docker-tag.outputs.tag }}
            ghcr.io/${{ github.repository }}:${{ github.sha }}
          build-args: |
            NEXT_PUBLIC_APP_URL=https://preview.jpvbootcamp.com
            APP_BASE_URL=https://preview.jpvbootcamp.com
            NEXT_PUBLIC_SERVER_URL=https://preview.jpvbootcamp.com
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Image pushed to GHCR
        run: |
          echo "✅ Docker image built and pushed to GHCR:"
          echo "   ghcr.io/${{ github.repository }}:${{ steps.docker-tag.outputs.tag }}"
          echo ""
          echo "External preview automation may pull this image after separate deployment authorization."
          echo "Runtime must remain STARTUP_MODE=application-only unless database deployment is separately approved."
